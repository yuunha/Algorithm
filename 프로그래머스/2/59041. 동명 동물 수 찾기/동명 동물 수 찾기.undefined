-- 코드를 입력하세요
-- 6개 : ANIMAL_ID, ANIMAL_TYPE, DATETIME, INTAKE_CONDITION, NAME, SEX_UPON_INTAKE
-- 1. 두 번 이상 쓰인 이름과 해당 이름이 쓰인 횟수를 조회
-- 2. 이름이 없는 동물은 집계에서 제외
-- 3. 결과는 이름 순으로 
# SELECT * FROM ANIMAL_INS ORDER BY NAME;

SELECT NAME , COUNT(ANIMAL_ID) AS 'COUNT' 
FROM ANIMAL_INS
WHERE NAME IS NOT NULL 
GROUP BY NAME
HAVING COUNT(ANIMAL_ID) > 1
ORDER BY NAME;